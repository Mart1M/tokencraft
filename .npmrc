# Flat node_modules so Next.js standalone bundles resolve `next` and peers
# when copied into the CLI package (pnpm symlinks break in isolation).
# https://github.com/vercel/next.js/issues/48017
node-linker=hoisted
